This option defines how the firewall will be configured on the target system:
+ firewall --enabled --service=ssh,cups
* network --device=eth0 --bootproto=dhcp
* lang en_US.UTF-8
* selinux --enforcing
* change - firewall
