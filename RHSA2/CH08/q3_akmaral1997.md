To store the root certificate authorities that can validate the SSL certificates used to identify LDAP servers
+ ` /etc/openldap/cacerts `
* ` /etc/krb5.conf `
* ` /etc/sssd/sssd.conf `
* ` /etc/nsswitch.conf `
* ` /etc/pam.d.conf `
