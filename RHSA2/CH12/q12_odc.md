One of the basic step for accessing `SMB` share:

* `Authentication to 'SMB' shares `
+ `Identify the remote share to access`
* `Enable and start the autofs service `
* `Mapping to secure access `
* `'SMB' access to basic share `
