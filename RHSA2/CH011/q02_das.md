Have a point of mount called mountpoint, how to manually unmount this section?

* `sudo automount /mountpoint`
+ `sudo umount /mountpoint`
* `sudo mount /mountpoint`
* `sudo umount /var/mountpoint`
* `sudo nfsmount /mountpoint`
