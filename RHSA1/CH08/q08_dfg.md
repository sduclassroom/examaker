Command to Completely disable a service from being started, both
manually and at boot

+ `systemctl mask`
* `systemctl stop unit`
* `systemctl restart unit`
* `systemctl start unit`
* `systemctl reload unit`
* `systemctl mask unit`
