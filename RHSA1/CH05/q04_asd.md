 Which command removes the user from /etc/passwd but leaves the home directory intact by default ?
+ `userdel username`
* `delete  username`
* `remove username`
* `userdelete userid`
* `useradd username`

