Which command is used to change a group name to a GID mapping.

* `groupdel command`
+ `groupmod command`
* `usermod command`
* `usermod -aG groupname username command`
* ` groupadd groupname command`
