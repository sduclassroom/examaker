A ssh command is useful for ?

 * `archiving the file`
 * `to find the directory`
 + `securely running shell commands  on remote systems`
 * `copy the  local file on desktopX`
 * `copy the local files on serverX`
