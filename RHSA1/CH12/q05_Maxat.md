How to connect to the serverX machine as root by using ssh?
+ `[root@desktopX configsync]# ssh root@serverX`
* `[root@desktopX configsync]# ssh rootserverX`
* `[root@desktopX configsync]# ssh root@serverX`
* `[root@desktopX configsync]# ssh root`

